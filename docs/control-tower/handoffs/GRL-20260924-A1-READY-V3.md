# github-runner-local — A1-ready handoff V3

## 1. Phase

Independent Opus 5.5 planning completed and Control Tower reviewed it. Phase: **A1_READY / AWAITING_IMPLEMENTATION_WORKER**. The product is still not installable and no real runner/login/UAC/service/job has been exercised.

## 2. Authority

Read main `AGENTS.md` → `docs/CURRENT.md` → this handoff through its end marker → Issue #1 latest comments → Issue #2 plan and CT review → active Issue #4.

Issue #1 is coordination/claims. Issue #2 is completed planning/review. Issue #4 is the only currently authorized implementation packet.

## 3. Refs and planning lineage

Pre-review main was `7f3cc5a6aea4a08cae93e069e756e5bf5ac6da4e`.
Rough-design DRAFT PR #3 remains open/unmerged, branch `design/grl-001-foundation-v0`, head `0f009a1fefefeec45c40ced8f172a0373746fe12`.

Opus claim `5807712960` was released by `5807786134`.
Opus plan is Issue #2 comment `5807784901`, sentinel `END_OF_GRL_OPUS55_PLAN key=GRL-001-OPUS55-PLAN-20260924 sections=9`.
CT review is Issue #2 comment `5807941253`, result `A1_READY_TO_PACKET`.
CT review claim is Issue #1 comment `5807925977`; its release comment pins the final main containing this V3 handoff.

## 4. Review disposition

The plan is **accepted for A1 with eight carry-forward corrections**, not blanket-approved for live integration.

A1 corrections that directly bind the worker:
- Windows path semantics are explicit/host-independent while tested on Linux.
- No GitHub-hosted Actions; worker-local compute only.

Later corrections/gates:
- GitHub App visibility remains owner decision.
- multi-repo credential minting needs separate design;
- runner initial pin + auto-update requires runtime version gating;
- reporting-only rerun output reuse remains unverified;
- branch containment is not code trust;
- issue-comment activation must preserve/prove the default-branch trigger requirement.

## 5. Current accepted technical direction

One runner initially. Portable/trusted-code mode before service mode. Public product repo stays separate from later private execution surface. No direct chat-to-PC shell. No arbitrary shell command comments. No source auto-push. No admin CI by default.

For later login, GitHub App device flow remains reviewed direction, not yet owner-authorized creation. For later dispatch, a private mailbox issue + reviewed default-branch workflow remains proposed and must be proven.

## 6. Active A1 packet

Issue #4 / GRL-002 implements only:
- deterministic wizard state machine and non-secret journal model;
- settings v1;
- `DiskAdmission`;
- host-independent `WindowsPathPolicy` over fake filesystem metadata;
- request/result/settings schemas + validators.

Allowed/forbidden files and tests are authoritative in Issue #4 and `docs/tasks/GRL-002-a1-core-contracts.md`. Branch: `feat/grl-a1-core-contracts`. One DRAFT PR. No merge.

## 7. A1 proof

Required worker-local proof only:
`dotnet build -warnaserror`; `dotnet test`; exact test count; deterministic/no network.

Linux success is `LOCAL_CHECKED` only and does not establish Windows filesystem, auth, runner or service behavior.

## 8. Open owner decisions for later work

No decision is needed to start A1.

Before live registration/acceptance:
- OD-1: create/reuse dedicated private execution repo (reviewed default: new dedicated private repo);
- OD-2: create GitHub App and choose acceptance-stage visibility;
- OD-7: authorize office-laptop enrollment.

OD-3 service mode is deferred. OD-4 license and OD-6 signing gate release. OD-5 is currently **no GitHub-hosted Actions** until the owner explicitly changes it.

## 9. Evidence and platform review

CT verified current GitHub docs during review: repo registration token endpoint supports GitHub App user access tokens with Administration(write); token lifetime is one hour; matching self-hosted jobs queue up to 24 hours; runner auto-update is default; `issue_comment` requires its workflow on the default branch; `GITHUB_TOKEN`-created events generally do not recursively start workflows; GitHub App device flow and eight-hour expiring user tokens are documented.

These are `SOURCE_VERIFIED` platform facts, not live project acceptance.

## 10. Next action and stop

One bounded implementation worker may claim Issue #4, implement A1, test in its own compute, publish one DRAFT PR and handoff, release claim, and stop.

Do not launch B/C/D/E/G/F/H from this handoff. No worker was launched by the CT review itself.

END_OF_GRL_HANDOFF key=GRL-20260924-A1-READY-V3 sections=10
