const fs=require('node:fs'),path=require('node:path');const root=path.resolve(process.argv[2]||path.join(__dirname,'../../..'));const here=path.resolve(__dirname,'..');for(const n of ['grl.request.v1.schema.json','grl.result.v1.schema.json']){const a=fs.readFileSync(path.join(root,'schemas',n)),b=fs.readFileSync(path.join(here,'schemas',n));if(!a.equals(b)){console.error(`schema drift: ${n}`);process.exitCode=1}}if(!process.exitCode)console.log('SCHEMA_MIRRORS_PASS');
